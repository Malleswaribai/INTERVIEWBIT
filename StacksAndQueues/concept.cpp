/*
    stacks: stack is a data structure, which will point to one element in the stack , which is at the top .
    either to remove or to insert or to access. 
    follows principle last in last out
    time complexity is o(1) for every operation. 

    cout<<"stacks"<<endl;
    stack<int> s; 
    s.push(1); 
    s.push(2);
    cout<<s.top()<<endl;
    s.pop(); 
    cout<<s.size()<<endl;
    
    queue: queue is a data strucutre which will follow principle first in first out. 
    we can access the first element either remove or print, but we can always insert an element at the last;
    time complexity is o(1) for every operation; 
    
    cout<<"queues"<<endl;
    queue<int> q; 
    q.push(1) ;
    q.push(2) ;
    cout<<q.front()<<endl;
    q.pop();
    cout<<q.size()<<endl; 
    cout<<q.first();
    

*/